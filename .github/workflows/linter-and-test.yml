name: Linter and test

on:
  pull_request:
    branches:
      - 'main'
jobs:
  Linter-And-Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci
      - run: npm run lint
      - run: npm run test:unit
